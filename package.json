{
    "devDependencies": {
        "esbuild": "^0.6.10"
    },
    "scripts": {
        "build": "npx esbuild --bundle ./src/app.ts --outdir=out"
    },
    "dependencies": {
        "rxjs": "^6.6.0"
    }
}
